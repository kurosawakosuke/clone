<!DOCTYPE html>
<html lang="ja">

<div class="container">
    <%= image_tag('char.png', :class => "main-header") %>
  <h1>Listing Blogs</h1>
  <p><%= notice %></p>

  <div class="row">
    <% @blogs.each do |blog| %>
      <div class="col-xs-12 col-md-6 blog-panel">
        <p>投稿者名：<%= blog.user.name %></p>
        <p><%= blog.content %></p>
        <div class="blog-links">
          <% case logged_in? %>
            <% when current_user.id != @user.id %>
              <%= link_to '詳細を確認する', blog_path(blog.id), class: 'btn btn-default' %>
              <%= link_to "つぶやきを編集する", edit_blog_path(blog.id) , data: { confirm: '本当に編集していいですか？' }, class: 'btn btn-info'%>
              <%= link_to 'つぶやきを削除する', blog_path(blog.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } , class: 'btn btn-danger'%>
            <% else %>
              <%= link_to '詳細を確認する', blog_path(blog.id), class: 'btn btn-default' %>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
